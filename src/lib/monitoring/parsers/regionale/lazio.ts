/** Parser dedicato — Regione Lazio. */
import { creaParserRegionaleGenerico } from "./factory";

export const parserRegioneLazio = creaParserRegionaleGenerico({
  ente: "Regione Lazio",
  regione: "Lazio",
});
